# Slots

Add examples and descriptions of the component features here.

<code-bubble>

```html
<wc-slots tag="my-element" component-name="MyElement"></wc-slots>
```

```tsx
import { WcSlots } from 'lit-starter-kit/react';

export default () => {
  return (
    <>
      <WcSlots tag="my-element" componentName="MyElement"></WcSlots>
    </>
  );
};
```

</code-bubble>
